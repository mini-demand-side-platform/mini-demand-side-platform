apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bidding-server-ingress
spec:
  rules:
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bidding-server
                port:
                  number: 8003
